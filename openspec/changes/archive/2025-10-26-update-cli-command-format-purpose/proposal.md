## Why
The `cli-command-format` specification still contains a placeholder Purpose value following archival of the originating change. Updating the Purpose keeps the capability documentation self-describing.

## What Changes
- Replace the `cli-command-format` Purpose section with a concise statement explaining the standard layout expectations for CLI command packages.

## Impact
- Affected specs: `cli-command-format`
- Affected code: specification only
